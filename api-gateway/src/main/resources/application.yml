spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/auth/**  # Все запросы на аутентификацию будут перенаправляться на отдельный сервис аутентификации
debug: true